// @ts-check
import { defineConfig } from "astro/config";

export default defineConfig({
  // Set this to your real domain once you have one — it's used for
  // canonical URLs and social-share metadata.
  site: "https://example.netlify.app",
});
